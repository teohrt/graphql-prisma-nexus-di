import * from "../generated/prisma.graphql";

type Query {
    user(id: ID!): User
    users: [User]
}

type Mutation {
    createuser(uid: String, name: String, email: String, photoURL: String, isNewUser: Boolean): User
}
